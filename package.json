{
  "name": "truentity-playwright-e2e",
  "version": "1.0.0",
  "description": "Claude Code + Playwright MCP E2E automation workspace for Truentity Health QA",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.18",
    "@playwright/test": "^1.49.0",
    "@types/node": "^26.3.0",
    "dotenv": "^16.4.5",
    "typescript": "^7.0.2"
  }
}
